#Meeting.findMaxPrevious=SELECT m FROM Meeting m WHERE m.submitDate IN (SELECT MAX(submitDate) FROM Meeting WHERE user.id=:meeting.user.id or user.room.id=:meeting.user.id AND submit_date <= ?)
#Meeting.findMinFollowing=  SELECT m FROM Meeting m WHERE m.submitDate IN (SELECT MAX(submitDate) FROM Meeting WHERE user.id=:meeting.user.id or user.room.id=:meeting.user.id AND submit_date >= ?)